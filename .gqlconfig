{
  schema: {
    files: "schema-merged.graphql"
  },
  query: {
    files: [
      {
        match: { include: 'src/**/*.t*' },
        parser: [ 'EmbeddedQueryParser', { startTag: 'gql`', endTag: '`' } ],
        validate: {
          extends: 'gql-rules-query',
          rules: {
            KnownDirectives: 'off'
          }
        }
      }
    ]
  }
}
