/**
 * Specifies requirements around the selection process of Entries by new users.
 * Users are asked to select Entries during their onboarding process.
 */
export interface IEntrySelectMeta {
  minEntriesCount: number;
  maxEntriesCount: number;
}
