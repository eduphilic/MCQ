/**
 * Filenames of index card background images for use on the landing page in the
 * AttemptCourseCard components.
 */
export const landingAttemptCourseCardBackgrounds = [
  "air force1.png",
  "army1.png",
  "assam rifles1.png",
].map(f => encodeURIComponent(f));
