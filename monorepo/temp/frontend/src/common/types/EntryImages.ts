/**
 * A mapping of "entry" names to their images.
 *
 * These should later be replaced with server uploaded files controlled from the
 * admin dashboard.
 */
export type EntryImages = Record<
  | "AirForce"
  | "Army"
  | "AssamRifles"
  | "BSF"
  | "CoastGuard"
  | "CRPF"
  | "ITBP"
  | "MNS"
  | "Navy"
  | "Officer"
  | "SSB",
  string
>;
